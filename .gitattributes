# Generated by osbap_data/tools/deploy_factorviz.py -- do not edit.
#
# Disable ALL end-of-line conversion. The data files are written on Windows with CRLF and
# are byte-identical to the CSVs inside the published release archives; README.md says so.
# Without this line git stores LF, the served file comes out one byte per line shorter
# (measured: 820,657 B on disk vs 820,029 B served), and that claim quietly stops being true.
* -text
